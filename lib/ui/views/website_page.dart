import 'package:flutter/material.dart';

class WebsitePage extends StatelessWidget {
  const WebsitePage({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
